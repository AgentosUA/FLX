let rootNode = document.getElementById('root');

let addNew = () => {
    let text = document.getElementsByClassName('list-input')[0].value;
        
}

let button = document.getElementById('button');
button.addEventListener('click', addNew, false);